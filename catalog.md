# カタログ

公式リファレンス: https://reference.makeshop.jp/creator-mode/contents/catalog/index.html

⚠️ 推測サンプル: 公式リファレンス上でサンプルコードが確認できないタグは、タグ名と概要から使用例を推測して補完しています。

# カタログ情報

## $catalog.number_id

### 概要
カタログ番号入力フォーム用データID

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<div><{$catalog.number_id}></div>
```

#### HTML変換後
```html
<!-- $catalog.number_id: カタログ番号入力フォーム用データID -->
```

## $catalog.quantity_id

### 概要
数量入力フォーム用データID

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<div><{$catalog.quantity_id}></div>
```

#### HTML変換後
```html
<!-- $catalog.quantity_id: 数量入力フォーム用データID -->
```

## $catalog.search_url

### 概要
カタログ商品検索URL

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$catalog.search_url}></p>
```

#### HTML変換後
```html
<p>カタログ商品検索URL</p>
```

## $catalog.cart_entry_url

### 概要
かごに入れるURL

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$catalog.cart_entry_url}></p>
```

#### HTML変換後
```html
<p>かごに入れるURL</p>
```

## $catalog.name_html

### 概要
商品名

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$catalog.name_html}></p>
```

#### HTML変換後
```html
<p>商品名</p>
```

## $catalog.special_display_html

### 概要
商品別特殊表示

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$catalog.special_display_html}></p>
```

#### HTML変換後
```html
<p>商品別特殊表示</p>
```

## $catalog.price_html

### 概要
販売価格

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$catalog.price_html}></p>
```

#### HTML変換後
```html
<p>販売価格</p>
```

## $catalog.price_excluded_tax_html

### 概要
税抜き価格

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$catalog.price_excluded_tax_html}></p>
```

#### HTML変換後
```html
<p>税抜き価格</p>
```

## $catalog.tax_html

### 概要
税

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$catalog.tax_html}></p>
```

#### HTML変換後
```html
<p>税</p>
```

## $catalog.image_S_id

### 概要
商品縮小画像表示用データID

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<div><{$catalog.image_S_id}></div>
```

#### HTML変換後
```html
<!-- $catalog.image_S_id: 商品縮小画像表示用データID -->
```

## $catalog.image_M_id

### 概要
商品普通画像表示用データID

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<div><{$catalog.image_M_id}></div>
```

#### HTML変換後
```html
<!-- $catalog.image_M_id: 商品普通画像表示用データID -->
```

## $catalog.image_L_id

### 概要
商品画像表示用データID

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<div><{$catalog.image_L_id}></div>
```

#### HTML変換後
```html
<!-- $catalog.image_L_id: 商品画像表示用データID -->
```
