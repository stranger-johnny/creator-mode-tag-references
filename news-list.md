# お知らせ一覧

公式リファレンス: https://reference.makeshop.jp/creator-mode/contents/news-list/index.html

⚠️ 推測サンプル: 公式リファレンス上でサンプルコードが確認できないタグは、タグ名と概要から使用例を推測して補完しています。

# お知らせ一覧

## $news.has_item

### 概要
お知らせが登録されているかどうか（真偽値）

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.has_item}></p>
```

#### HTML変換後
```html
<!-- $news.has_item: true / false -->
```

## $news.list[i].title

### 概要
お知らせタイトル

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.list[i].title}></p>
```

#### HTML変換後
```html
<p>お知らせタイトル</p>
```

## $news.list[i].url

### 概要
お知らせURL

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.list[i].url}></p>
```

#### HTML変換後
```html
<p>お知らせURL</p>
```

## $news.list[i].content

### 概要
お知らせ内容

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.list[i].content}></p>
```

#### HTML変換後
```html
<p>お知らせ内容</p>
```

## $news.list[i].date.year

### 概要
投稿日時（年）

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.list[i].date.year}></p>
```

#### HTML変換後
```html
<p>投稿日時（年）</p>
```

## $news.list[i].date.month

### 概要
投稿日時（月）

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.list[i].date.month}></p>
```

#### HTML変換後
```html
<p>投稿日時（月）</p>
```

## $news.list[i].date.day

### 概要
投稿日時（日）

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.list[i].date.day}></p>
```

#### HTML変換後
```html
<p>投稿日時（日）</p>
```

## $news.list[i].date.hour

### 概要
投稿日時（時）

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.list[i].date.hour}></p>
```

#### HTML変換後
```html
<p>投稿日時（時）</p>
```

## $news.list[i].date.minute

### 概要
投稿日時（分）

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.list[i].date.minute}></p>
```

#### HTML変換後
```html
<p>投稿日時（分）</p>
```

# ページャー

## $news.pager.is_first

### 概要
表示中のページが最初のページかどうか（真偽値）

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.pager.is_first}></p>
```

#### HTML変換後
```html
<!-- $news.pager.is_first: true / false -->
```

## $news.pager.first_url

### 概要
最初のページURL

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.pager.first_url}></p>
```

#### HTML変換後
```html
<p>最初のページURL</p>
```

## $news.pager.prev_url

### 概要
前のページURL

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.pager.prev_url}></p>
```

#### HTML変換後
```html
<p>前のページURL</p>
```

## $news.pager.is_last

### 概要
表示中のページが最後のページかどうか（真偽値）

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.pager.is_last}></p>
```

#### HTML変換後
```html
<!-- $news.pager.is_last: true / false -->
```

## $news.pager.last_url

### 概要
最後のページURL

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.pager.last_url}></p>
```

#### HTML変換後
```html
<p>最後のページURL</p>
```

## $news.pager.last_number

### 概要
最終ページ数

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.pager.last_number}></p>
```

#### HTML変換後
```html
<p>最終ページ数</p>
```

## $news.pager.next_url

### 概要
次のページURL

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.pager.next_url}></p>
```

#### HTML変換後
```html
<p>次のページURL</p>
```

## $news.pager.list[i].number

### 概要
ページ番号

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.pager.list[i].number}></p>
```

#### HTML変換後
```html
<p>ページ番号</p>
```

## $news.pager.list[i].url

### 概要
ページURL

### サンプルコード（⚠️ 推測）

#### クリエーターモード
```php
<p><{$news.pager.list[i].url}></p>
```

#### HTML変換後
```html
<p>ページURL</p>
```
